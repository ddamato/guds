import { addons } from '@storybook/addons';
addons.setConfig({
  previewTabs: {
    canvas: 'Canvas',
    'storybook/docs/panel': 'Documentation',
    'storybookjs/notes/panel': 'Guidelines',
  },
});
