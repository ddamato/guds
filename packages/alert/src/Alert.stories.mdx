import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { withDesign } from 'storybook-addon-designs'
import Alert from './index.js';

<Meta title="Design System/Alert" component={Alert} decorators={[withDesign]} />

# Alert

With `MDX` we can define a story for `Alert` right in the middle of our
Markdown documentation.

<Canvas>
  <Story
    name='Default'
    args={{ children: 'Default Alert' }}
    parameters={{
      jest: ['alert.test.js'],
      design: {
        type: 'figma',
        url: 'https://www.figma.com/file/LKQ4FJ4bTnCSjedbRpk931/Sample-File',
      },
    }}>
      { args => <Alert { ...args } /> }
  </Story>
</Canvas>

<ArgsTable story='Default'/>

<Canvas>
  <Story
    name='Critical'
    args={{ children: 'Critical Alert', design: 'critical' }}
    parameters={{
      jest: ['alert.test.js'],
      design: {
        type: 'figma',
        url: 'https://www.figma.com/file/LKQ4FJ4bTnCSjedbRpk931/Sample-File',
      },
    }}>
      { args => <Alert { ...args } /> }
  </Story>
</Canvas>

<ArgsTable story='Critical'/>

<Canvas>
  <Story
    name='Warning'
    args={{ children: 'Warning Alert', design: 'warning' }}
    parameters={{
      jest: ['alert.test.js'],
      design: {
        type: 'figma',
        url: 'https://www.figma.com/file/LKQ4FJ4bTnCSjedbRpk931/Sample-File',
      },
    }}>
      { args => <Alert { ...args } /> }
  </Story>
</Canvas>

<ArgsTable story='Warning'/>

