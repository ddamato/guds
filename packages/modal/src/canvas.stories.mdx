import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { withDesign } from 'storybook-addon-designs';
import guidelines from './guidelines.md';
import Modal from './index.js';

<Meta title="Design System/Modal" component={Modal} decorators={[withDesign]} />

# Modal

With `MDX` we can define a story for `Modal` right in the middle of our
Markdown documentation.

<Canvas>
  <Story
    name="Default"
    args={{ children: 'Default Modal' }}
    parameters={{
      jest: ['modal.test.js'],
      design: {
        type: 'figma',
        url: 'https://www.figma.com/file/LKQ4FJ4bTnCSjedbRpk931/Sample-File',
      },
      notes: { guidelines }
    }}>
      { args => <Modal { ...args } /> }
  </Story>
</Canvas>